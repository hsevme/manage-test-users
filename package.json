{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:clopen": "sudo rm -rf node_modules/cypress && npm install && npx cypress open",
    "cy:open": "npx cypress open"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.8.1",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "cypress-real-events": "^1.12.0"
  }
}
